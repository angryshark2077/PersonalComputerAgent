# Browser Extension scaffold

S5 scope. The extension may emit URL/title/domain metadata through the allowlisted Native Messaging Host.

It must not read cookies, passwords, form contents or arbitrary page body.
